braintrust:
  project_name: load-testing-project
  api_url: https://api.braintrust.dev

evaltest:
  run: True
  project_id: <id> # if null a new project will be created
  name: test-large #Name for this eval
  dataset:
    name: test-large-dataset
    description: A large test dataset for conducting a load test of a large eval
    initialize_dataset: true
    size: 100 # Number of rows in dataset
    flush_batch_size: 25 # How many events to write to the dataset at a time
    shape: # Currently not read, but below is the sample data shape used
      input: |
        {
          "name": str,
          "address": str,
          "message": str
        }
      expected: |
        {
          "sentiment": ["positive", "negative", "neutral"],
          "summary": str
        }
      metadata: |
        {
          "model": ["sonnet-4", "sonnet-5", "gpt-4", "gpt-5"],
          "tokens": int
        }

loadtest:
  run: True
  locustfile_path: loadtest/run.py
  autostart: True # Whether the test should start automatically or be triggered from the web UI. 
  web_ui_port: 8089
  params:
    peak_concurrency: 50 # Number of users at peak load
    ramp_up: 2 # new users spawned per second
    run_time: 30s #e.g. 20s, 3m, 2h, 1h20m, 3h30m10s, etc.
    wait_time:  #amount of time to wait between simulating a new request from a user (a value is chosen at random bw the bounds)
      min: 1 
      max: 5